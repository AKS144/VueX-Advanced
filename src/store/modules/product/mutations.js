export const SET_PRODUCTS = (state, products)=>{
    state.products = products; //from that state(SET_PRODUCTS) products updated
}

export const SET_PRODUCT = (state, product)=>{//state and product is parameter and state.product is state object
    state.product = product; //from that state(SET_PRODUCT) products updated
}